'use strict'

const common = require('./common')
const findpass = require('./findpass')
const user = require('./user')
const admin = require('./admin')

module.exports = { common, findpass, user, admin }